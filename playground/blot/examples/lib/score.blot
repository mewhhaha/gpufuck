// A module with a parameter. It has no authority beyond the record it is given.

module capabilities;

open {} = (@import "blot:prelude") ();

return {
  .run = capabilities.base + capabilities.bonus;
  .scaled = capabilities.base * 2;
};
