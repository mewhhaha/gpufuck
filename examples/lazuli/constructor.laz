data Pair a b = Pair(first: a, second: b);

fn completePair partial = partial 32;

fn main =
  let withTen = Pair 10 in
  case completePair withTen of
    | Pair(first, second) -> first * 100 + second
  end;
