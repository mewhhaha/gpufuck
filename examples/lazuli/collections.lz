-- List literals lower to the built-in lazy List constructors.

let sum : List Int -> Int = values =>
  case values of
    | Nil -> 0
    | Cons(head, tail) -> head + sum tail
  end;

fn main = sum ([10, 12, 20]);
